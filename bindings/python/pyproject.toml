[project]
name = "glyphrush"
version = "0.1.0"
description = "Thin Python wrapper for the Glyphrush native PDF parser CLI"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
